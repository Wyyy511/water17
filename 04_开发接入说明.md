# WaterPulse 首页开发接入说明

版本 homepage-ui-1.1 · 2026-09-13

## 1 交付目标和适用边界

以当前 WaterPulse 首页为基础，增加具体用途说明、成果预览与四步示例体验。现有蓝绿色视觉方向和直接提问入口保留，缩短大标题占用，避免长介绍被输入框遮挡。

这是独立的前端设计交付，未取得原站源码，因此不会假定原站函数、DOM选择器、框架、状态管理或接口请求结构。整合前先在原代码中定位真实入口。

2026-09-13 本会话已观察到的首页控件为：文字输入、上传结构化数据Excel/CSV、Excel模板、语音输入、体验示例、新对话，以及模型连接状态。页面上出现控件只证明入口存在，不代表相关能力已通过完整流程验收。

## 2 页面和现有系统的分工

| 模块 | 本包可复用内容 | 必须由原站提供或验证 |
| --- | --- | --- |
| 首页 | 介绍、布局、任务提示、成果预览、样式 | 当前产品能力与文案一致性 |
| 提问 | 文本框、空输入提示、按钮、失败保留输入 | 会话创建/恢复、发送、取消、结果渲染 |
| 上传 | 上传入口与说明 | 原文件选择、支持格式、大小校验、解析、映射确认 |
| 模板 | 下载入口 | 当前数据合同对应的真实Excel模板 |
| 示例 | 本地四步教学交互和说明下载 | 如改用线上示例，须绑定同版本计算与来源记录 |
| 分析过程 | 四阶段信息架构 | 真实任务事件与缺口状态，不能由定时器伪造完成 |
| 分析结果 | 贡献排序、解释、情景比较的展示参考 | 计算工具返回数据、来源、假设、缺口、版本及导出 |

## 3 推荐接入顺序

1. 保留原站可运行版本，在独立分支或副本中改首页，记录回退位置。
2. 确认原站空会话首页、聊天工作区、上传入口、模板入口及“新对话”的真实函数与生命周期。
3. 提取 `index.html` 中的 `#wp-home` 容器。宿主页继续拥有 `html/head/body`，不要嵌套整份HTML，也不要照搬独立页面的body样式。
4. 引入CSS。全部规则已限定在 `#wp-home`，但宿主的全局button、dialog、h1、textarea规则仍可能影响它；对冲突做局部覆盖。避免同时加载重复的首页样式。
5. 按当前框架选择移植方式。原生HTML可以使用现成JS；React/Vue应把结构和事件转换为组件，不要让框架与这份JS同时修改同一个弹窗内容。
6. 注册 `window.WaterPulseHomepageBridge.handleAction()`，或采用同义组件事件。把 `submit/upload/template` 分别接到原站真实功能。
7. 进入分析时，收起介绍、预览、流程等首页内容，复用原聊天工作区。保留本次输入、文件状态与会话，避免产生两套输入框或两个同时运行的会话。
8. 新对话复用原逻辑；只有确实创建新会话后，才回到首页。已有会话中的模型状态、取消和重试功能继续保留。
9. 上线前完成第9节验收。生产页面删除“首页交互原型”页脚；示例中的“模拟采购情景”和假设标记必须保留。

## 4 按钮适配合同

原型没有任何 `fetch`、远程资源、密钥、上传读取、localStorage 或线上端点。业务连接唯一入口为宿主主动注册的适配器。

```js
window.WaterPulseHomepageBridge = {
  async handleAction({ action, payload, source, version }) {
    // action: submit | upload | template
    // submit 的 payload.text 是用户本次输入；不要改成示例文字。
    // source 固定为 waterpulse-home；version 为 1.0。
    // 调用真实宿主函数，并返回其 Promise；失败应 reject。
  }
};
```

`integration.example.js` 提供适配器工厂，里面示范的宿主函数名不是当前项目真实函数名。开发同学须查原代码替换。未经接入时，原型提示仅支持示例，不能显示“已上传”“已计算”或“服务已连接”。

| 交互 | 用户动作 | 正式站应发生的行为 |
| --- | --- | --- |
| 开始分析 | 输入问题后点击 | 校验空文本，传给原会话入口，切换工作区；失败时保留输入并允许重试 |
| 两个任务提示 | 点击提示词 | 只填入文本框，允许编辑；不自动调用模型 |
| 上传Excel/CSV | 点击 | 打开原站上传选择器，使用原校验与字段映射流程；不要只显示文件名就声称解析完成 |
| 下载模板 | 点击 | 使用原站模板下载逻辑；核对模板字段、单位与计算引擎版本 |
| 体验示例 | 点击 | 打开本地教学示例；若后续改成在线示例，需显式区分两种状态 |
| 查看示例依据 | 点击 | 显示完整教学输入、公式、数据身份与边界 |
| 下载示例说明 | 第四步点击 | 下载固定教学说明TXT，不是正式企业风险报告 |
| 带着自己的问题开始 | 第四步点击 | 关闭示例并聚焦原输入，保留用户原有文字 |
| Esc/关闭按钮 | 弹窗内操作 | 关闭弹窗并回到触发它的控件；按Tab不能逃出模态框 |

语音输入在当前首页可见。本方案把它作为输入区次要工具，开发时保留原入口，确认转写可用后放在上传按钮旁；本地原型没有请求麦克风权限。模型名称与连接状态移到工作区或设置位置，仍需提供实际异常提示，不能以固定绿色标记代替状态检查。

## 5 示例数据与演算

此示例专为讲解界面设计，不复用研究报告的PRWI、线上演示结果或未经核验的真实地区值。

| 项目 | 巴西示例节点 | 广西示例节点 |
| --- | --- | --- |
| 原材料 | 甘蔗 | 甘蔗 |
| 固定示例采购占比 | 60% | 40% |
| 教学指定节点强度 | 0.40 | 0.50 |
| 加权贡献 | 0.240 | 0.200 |
| 贡献份额 | 54.5% | 45.5% |

加权贡献 = 占比 × 教学指定强度；份额 = 节点贡献 / 两地贡献合计。只演示组合加权关系，不实现正式三路径风险模型。首页图、弹窗初始值与示例说明的数字一致；滑块是独立探索，原始基准不被覆盖。

第三步滑块范围10%—90%，每次变化5个百分点，另一节点补足100%。两地教学强度固定。比如巴西50%时，两地贡献为0.200/0.250，广西贡献更高。此处不能把“风险贡献份额”叫作“减产比例”或“供应损失”。

第四步节点失效固定使用60% / 40%基准，不沿用滑块探索值：巴西供应损失比例50%，受影响供应占总需求30%；库存覆盖5%，替代供应覆盖10%，未满足需求15%。30%=5%+10%+15%。所有份额以同一时期总需求为分母。库存与替代容量均为假设，不从水风险指标推导。

若希望使用真实模型示例，请整体替换为同一受控运行的输入、结果、解释与导出，保留真实数据身份；不要只替换图中的几个数字。前端不得新增风险公式。

## 6 正式结果展示合同

以下为显示内容要求，不预设后端字段名。请以现有真实响应建立映射表。

- 运行结果：完整、部分、信息不足、冲突、错误；以及当前任务能否继续。
- 业务输入：材料、节点、参考时期、采购份额与分母；存在未知份额时独立显示。
- 重点节点：区分节点风险强度和采购加权贡献；贡献份额的分母必须明确。
- 数据质量：采购覆盖与可评分覆盖分别解释；未知不使用“0”或低风险绿色。
- 依据：来源、数据版本、参数/模型版本、假设、代理信息、缺口和生成时间。
- 情景对照：关联固定基准及影响范围；风险侧和供应侧的指标分开。
- 解释与导出：引用同一份计算结果。未经工具返回的数字不能由文案补出。

所有真实输出须移除教学设定内容；教学示例则继续在标题、图表和导出中保留身份标记。

## 7 能力核验与文案上线条件

| 展示内容 | 上线条件 | 条件未满足时 |
| --- | --- | --- |
| Excel/CSV结构化上传 | 完整验证上传、字段映射、校验与继续分析 | 保留已支持格式，明确失败和缺口状态 |
| ESG/PDF整份报告解析 | 独立验证抽取、原文证据、候选值确认 | 不写“上传ESG报告即可分析”；本版首页仅写Excel/CSV |
| 情景比较 | 至少对应情景可用，输入边界与输出已核对 | 指明所需数据和当前可用情景，不承诺四类全部自动完成 |
| 管理简报/正式报告导出 | 页面、工具结果与导出一致 | 不把本地示例TXT当成生产报告导出 |
| 数据来源可追溯 | 真实来源已贯穿查询、计算、解释与导出 | 展示缺口或未核验，不能写“全部权威数据” |
| 语音 | 转写接入与歧义确认通过 | 标记不可用或保留原系统实际状态 |

首页方法说明表达目标流程，开发合入前应按本表审校，已验证功能才能写为当前可用能力。避免“实时全球监测”“自动预测缺货”“一键最优采购”“100%可信”等无验收依据的表述。

## 8 视觉与适配

- 内容最大宽度1200px，外框最大1280px。桌面首页双栏，窄屏转单栏；输入在成果预览之前。
- 主色 `#137566`，主文字 `#172F3A`，次文字 `#536C75`，底色 `#F7FAF9`，示例面板 `#EAF3F0`；蓝色 `#3974AB` 用于区分第二节点。
- 桌面标题约35—48px，正文13—15px；窄屏标题35px。系统中文字体，无需下载字体。
- 原型当前断点：1000px、800px、480px。不得对整体页面使用固定高度或全局overflow:hidden。
- 页面正文可滚动；对话框内部可滚动，使用原生dialog进行焦点限制和Esc关闭。
- 按钮有可见焦点，输入有label，状态使用role=alert/status；颜色以外保留文字和数字解释。
- 生产适配注意375px、390px、768px、1024px、1440px和短高度窗口，尤其检查介绍被输入框遮挡问题。

## 9 合入后验收清单

### 首页和交互

- [ ] 新用户在首屏能看到用途、输入和示例入口；正文不会被固定输入框遮住。
- [ ] 任务提示只填入问题，不自动发送；空输入有清晰提示。
- [ ] 真实问题进入原站会话，不进入教学计算；输入和文件上下文不丢失。
- [ ] 上传、模板、新对话、语音（若可用）与旧功能一致。
- [ ] 服务错误可见，能够重试；不显示假的成功状态。
- [ ] 示例四步可前后切换，滑块修改只影响探索值，固定情景基准可辨认。
- [ ] 弹窗Esc/关闭、键盘Tab和焦点返回正常；窄屏无横向溢出。

### 真实分析结果

- [ ] 至少一组冻结输入贯通上传或文字、确认、基准、一个情景、解释及导出。
- [ ] 缺采购份额、未知地点、关键参数缺失、冲突值不会产生伪完整评分。
- [ ] 示例、代理和真实数据身份始终保留；数据来源与覆盖口径可查看。
- [ ] 三条风险路径与总值、贡献份额与分母正确；页面和导出一致。
- [ ] 节点失效的损失、库存、替代与未满足需求守恒，且不从风险指标直接猜损失。
- [ ] 只有合入后以上检查通过，才记录为部署验收完成。

## 10 内容依据

《研究报告最终提交版》执行摘要及第三章；《agent工作流【必看】》；《AI Agent 用户信息解析与数据查询开发接入任务书 V1.1》；2026-09-13当前首页只读观察。设计建议与示例数值不构成新研究证据。
